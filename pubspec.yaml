name: ai_doc_webapp
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.10.3

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  intl: any
  shimmer: ^3.0.0
  cupertino_icons: ^1.0.8
  http: ^1.6.0
  file_picker: ^10.3.7
  flutter_spinkit: ^5.2.2
  url_launcher: ^6.3.0   # added for download button functionality
  fl_chart: ^0.65.0
  shared_preferences: ^2.2.0
  speech_to_text: ^6.6.0
  flutter_tts: ^3.8.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true

